# -----------------------------------------------------------------------------
# Khalifa Foods · Environment configuration
# COPY this file to `.env.local` at the repo root, then symlink or copy into
# each app that needs its own (apps/web/.env.local, apps/desktop/.env.local).
# NEVER commit .env.local — it's git-ignored.
# -----------------------------------------------------------------------------

# ─── Supabase (create free project at https://supabase.com) ─────────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGci...replace-with-anon-key
SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...replace-with-service-role-key
SUPABASE_PROJECT_REF=your-project-ref
SUPABASE_DB_PASSWORD=your-db-password

# ─── Resend (transactional email — https://resend.com) ─────────────────────
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=orders@khalifafoods.com
RESEND_FROM_NAME="Khalifa Foods"

# ─── Public site config ────────────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_BUSINESS_NAME="Khalifa Foods"∏
NEXT_PUBLIC_BUSINESS_PHONE="0323-4748660"
NEXT_PUBLIC_BUSINESS_ADDRESS="18-F Commercial, PCHS, New Gourmet Bakery, DHA Lahore"

# ─── Tenant seed (single tenant in v1) ─────────────────────────────────────
NEXT_PUBLIC_TENANT_ID=00000000-0000-0000-0000-000000000001
NEXT_PUBLIC_BRANCH_ID=00000000-0000-0000-0000-0000000000a1

# ─── Sentry (optional — leave blank to disable) ────────────────────────────
NEXT_PUBLIC_SENTRY_DSN=

# ─── Rate limiting salt ────────────────────────────────────────────────────
RATE_LIMIT_SALT=change-this-random-string
∏